streamlit>=1.28.0
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
Pillow>=9.0.0
numpy>=1.23.0
torch
torchvision
# Prevent the full OpenCV package (needs libGL, fails on Streamlit Cloud)
# If a conflict appears, uninstall opencv-python and keep only headless
